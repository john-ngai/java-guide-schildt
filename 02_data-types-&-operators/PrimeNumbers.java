/**
 * Write a program that finds all the prime numbers between 2 and 100.
 * A prime number is a positive integer that can only be divisible by itself and 1.
 */

class PrimeNumbers {
    public static void println(String message) {
        System.out.println(message);
    }

    public static void main(String[] args) {
        println("All the prime numbers between 2 and 100:\n");

        for (int num = 2; num <= 100; num++) {
//            for (int i = 2; i < num; i++) {
//                if (num % i == 0) {
//                    println(String.valueOf(num));
//                    break;
//                }
//            }
        }
    }
}
